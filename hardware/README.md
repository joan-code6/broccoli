# hardware

This folder contains the MicroPython code for the hardware module.

`mfrc522.py`  is a library for the NFC reader. You will need to run ` uvx mpremote mip install github:smittytone/HT16K33-Python` to install the library for the LED matrix.
